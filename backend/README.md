# backend

Express API for the analytics frontend.

## Setup

```bash
nvm use   # from repo root
cp .env.example .env
# set RT_API_KEY
npm install
npm run dev
```

Default port: **3001** (`PORT` in `.env`).
