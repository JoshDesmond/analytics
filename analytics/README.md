# analytics (frontend)

Vite + React + TypeScript app for personal analytics.

## Development

Start [backend](../backend/) first, then:

```bash
npm install
npm run dev
```

API requests to `/api/*` are proxied to the backend in dev (see `vite.config.ts`).
